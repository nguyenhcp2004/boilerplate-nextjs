
NEXT_PUBLIC_URL=http://localhost:3000
NEXT_PUBLIC_API_ENDPOINT=http://localhost:3002
# Must match the backend's BETTER_AUTH_SECRET (source of truth: boilerplate-nestjs/.env).
# Used only for verifying the session cookie signature in proxy.ts.
BETTER_AUTH_SECRET=
